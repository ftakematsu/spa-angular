version: '3'
services:
  angular-app:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "4500:4200"
    volumes:
      - .:/app
    networks:
      - my_network
    command: ["npm", "start"]
networks:
  my_network: